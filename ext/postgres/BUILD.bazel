load(":postgres.bzl", "postgres_targets")

# PostgreSQL 18 - the default. Select 19 with --//ext:postgres_version=19 (see //ext:BUILD.bazel).
postgres_targets(
    name = "postgres",
    lib_source = "@postgres//:postgres_source",
)
