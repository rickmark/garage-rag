load("@rules_swift//swift:swift.bzl", "swift_library")

# ModelDownloadClient: the model-download XPC protocol, its Swift client and the downloader engine.
# The @objc protocol lives in the same module as the client so it is registered once.
swift_library(
    name = "ModelDownloadClient",
    srcs = glob(["**/*.swift"]),
    module_name = "ModelDownloadClient",
    visibility = ["//visibility:public"],
    deps = [
        "//macapp/Sources/PythonXPCService",
    ],
)
