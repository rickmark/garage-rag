*.pyc
.venv
.env
*.db
.build
macapp/build/
# Credential files (an LM Studio token, or a key left over from the retired cloud OCR)
*.key
anthropic.key

# Per-project config override (holds absolute paths and identity)
garage.json

*.bak-*

__pycache__

bazel-*

.bazelbsp/
*.xcodeproj
.output.txt
build/
# IDE data-source definitions embed the local database password
.idea/dataSources.xml
.idea/dataSources.local.xml

# Local Bazel overrides (see .bazelrc)
user.bazelrc

.ruff_cache/
.mypy_cache/
garage_python/dist/
