# The gRPC client lives in //garage_python/src/garage_rag/service, which depends
# back on this package. Both directions are function-local imports at runtime, so
# keep the Bazel dep graph acyclic by not materializing this one.
# gazelle:python_ignore_dependencies garage_rag.service.client

load("@aspect_rules_py//py:defs.bzl", "py_library")

py_library(
    name = "ingest",
    srcs = [
        "__init__.py",
        "chunking.py",
        "classify.py",
        "gateway.py",
        "materialize.py",
        "pipeline.py",
        "reconcile.py",
        "scanner.py",
        "walker.py",
    ],
    imports = ["../.."],
    visibility = ["//visibility:public"],
    deps = [
        "//garage_python/src/garage_rag/attribute",
        "//garage_python/src/garage_rag/config",
        "//garage_python/src/garage_rag/db",
        "//garage_python/src/garage_rag/extract",
        "//garage_python/src/garage_rag/proto",
        "@pypi//langchain_text_splitters",
        "@pypi//sqlalchemy",
    ],
)
